# ─────────────────────────────────────────────────────────────────────────────
# Clinical AI Agent — Python Requirements
# Install: pip install -r requirements.txt
#
# Notes:
#   - scispacy and the NER model require special install (see below)
#   - torch/transformers are GPU-optional; remove if running CPU-only
#   - After install, run: python -m spacy download en_core_web_lg
# ─────────────────────────────────────────────────────────────────────────────

# ── MCP server & agent orchestration ─────────────────────────────────────────
fastmcp>=3.0
mcp
openai>=2.0
uvicorn>=0.44
httpx>=0.26
httpx-sse>=0.4
anyio>=4.0

# ── Database & data processing ────────────────────────────────────────────────
duckdb>=1.5
pandas>=2.0
numpy>=1.24
python-dotenv>=1.0

# ── ML models ────────────────────────────────────────────────────────────────
scikit-learn>=1.4
xgboost>=2.0
shap>=0.44
joblib>=1.3

# ── RAG / Vector search ───────────────────────────────────────────────────────
chromadb>=1.0
sentence-transformers>=2.6
rank-bm25>=0.2.2

# ── NLP & de-identification ───────────────────────────────────────────────────
spacy>=3.7
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
phonenumbers>=8.0         # presidio dependency

# scispacy: install WITHOUT deps (avoids spaCy rebuild conflicts), then add model:
#   pip install --no-deps scispacy
#   pip install --no-deps https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_ner_bc5cdr_md-0.5.4.tar.gz

# ── PDF ingestion (guidelines RAG) ────────────────────────────────────────────
pdfplumber>=0.10
PyMuPDF>=1.23

# ── HTTP & web search ─────────────────────────────────────────────────────────
requests>=2.31
tldextract>=3.0

# ── GPU / deep learning (remove if CPU-only) ──────────────────────────────────
torch>=2.1
transformers>=4.38
safetensors>=0.4
tokenizers>=0.15
